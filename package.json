{
	"main": "lib/index.js",
	"version": "0.1.4",
	"type": "module",
	"dependencies": {
		"async-redis": "^1.1.7"
	},
	"scripts": {
		"build": "tsc --project tsconfig.json",
		"release-it": "yarn version --patch && git push --follow-tags"
	},
	"slug": "common/redis",
	"remote": "StandardHentaPlugins/redis",
	"devDependencies": {
		"@types/node": "^13.13.0"
	}
}
